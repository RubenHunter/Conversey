<div id="draftExitModal"
     class="fixed inset-0 hidden items-center justify-center bg-black/50">

    <div class="w-full max-w-md rounded-lg bg-white p-6 shadow-lg">
        <h2 class="text-lg font-semibold text-gray-900">
            Save draft before leaving?
        </h2>

        <p class="mt-2 text-sm text-gray-600">
            You have unsynced changes. Save draft and return to projects, or clear local draft.
        </p>

        <div class="mt-6 flex justify-end gap-3">
            <button id="draftExitCancel"
                    type="button"
                    class="rounded-md border px-4 py-2 text-sm text-gray-700 hover:bg-gray-100">
                Cancel
            </button>

            <button id="draftExitClear"
                    type="button"
                    class="rounded-md border border-red-200 px-4 py-2 text-sm text-red-700 hover:bg-red-50">
                Clear local draft
            </button>

            <button id="draftExitSave"
                    type="button"
                    class="rounded-md bg-primary px-4 py-2 text-sm text-white hover:bg-primary/90">
                Save draft
            </button>
        </div>
    </div>
</div>
