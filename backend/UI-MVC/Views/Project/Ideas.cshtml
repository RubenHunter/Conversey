<div id="ideas-root">
    <div class="flex items-center justify-center min-h-screen">
        <p class="text-gray-500">Laden van ideeën...</p>
    </div>
</div>

@inject Dictionary<string, string> Manifest
@section Scripts
{
    @{
        var jsFile = Manifest.GetValueOrDefault("components/ideas/pages/ideasPage.ts", "components/ideas/pages/ideasPage.ts");
        var cssFile = Manifest.GetValueOrDefault("components/ideas/pages/ideasPage.css", "");
    }
    @if (!string.IsNullOrEmpty(cssFile))
    {
        <link rel="stylesheet" href="/@cssFile" />
    }
    <script type="module" src="/@jsFile"></script>
}

