@model Conversey.UI_MVC.Models.Admin.AdminFormViewModel<Conversey.BL.Domain.Administration.Workspace>
@using Conversey.UI_MVC.Models
@inject AdminContext AdminContext
@{
    ViewData["Title"] = "Create Workspace";
    ViewData["WorkspaceName"] = "Conversey Platform";
    ViewData["Breadcrumbs"] = new (string Label, string? Url, bool IsCurrent)[]
    {
        ("Dashboard", AdminContext.GetDashboardUrl(), false),
        ("Workspaces", "/admin/workspaces", false),
        ("Create Workspace", null, true)
    };
    var formViewData = new ViewDataDictionary(ViewData)
    {
        ["ModalTitle"] = "Create workspace",
        ["SubmitLabel"] = "Create workspace"
    };
}

<div class="min-h-screen bg-background font-sans text-text">
    @await Html.PartialAsync("_AdminHeader")
    <main class="mx-auto w-full max-w-2xl p-8">
        @await Html.PartialAsync("_WorkspaceForm", Model, formViewData)
    </main>
</div>
