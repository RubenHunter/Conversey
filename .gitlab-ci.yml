stages:
  - validate
  - build
  - security
  - sign
  - deploy

# 1. Validatie op Merge Requests
validate-code:
  stage: validate
  image: mcr.microsoft.com/dotnet/sdk:8.0
  script:
    - echo "🔍 Code controleren..."
    - cd backend
    - dotnet restore
    - dotnet build --no-restore
  only:
    - merge_requests

# 2. Build & Push naar Productie (Google Artifact Registry)
build-backend:
  stage: build
  image: google/cloud-sdk:alpine
  before_script:
    - echo "$GCP_SERVICE_KEY" > gcp-key.json
    - gcloud auth activate-service-account --key-file=gcp-key.json
    - export GCP_PROJECT_ID=$(echo $GCP_PROJECT_ID | tr -d '\r\n')
    - gcloud config set project $GCP_PROJECT_ID
  script:
    - echo "🚀 Start Cloud Build voor de backend..."
    - gcloud builds submit backend/ --tag europe-west1-docker.pkg.dev/$GCP_PROJECT_ID/conversey-repo/backend:latest --quiet
  only:
    - main

# 3. Security Scanning & SBOM (DevSecOps)
security-scan:
  stage: security
  image: 
    name: aquasec/trivy:latest
    entrypoint: [""]
  variables:
    TRIVY_USERNAME: "oauth2accesstoken"
    TRIVY_PASSWORD: "$GCP_SERVICE_KEY" # Vereist access tot registry
  before_script:
    - apk add --no-cache curl
    # Geen shell in trivy image? We gebruiken de binary direct via entrypoint override
  script:
    - trivy image --severity HIGH,CRITICAL europe-west1-docker.pkg.dev/$GCP_PROJECT_ID/conversey-repo/backend:latest
  only:
    - main
  allow_failure: true

generate-sbom:
  stage: security
  image: alpine:latest
  before_script:
    - apk add --no-cache curl
    - curl -sSfL https://raw.githubusercontent.com/anchore/syft/main/install.sh | sh -s -- -b /usr/local/bin
  script:
    - echo "📦 Generating SBOM (Software Bill of Materials)..."
    - syft europe-west1-docker.pkg.dev/$GCP_PROJECT_ID/conversey-repo/backend:latest -o cyclonedx-json > sbom.json
  artifacts:
    paths:
      - sbom.json
    expire_in: 1 week
  only:
    - main

# 4. Image Signing (Cosign)
sign-image:
  stage: sign
  image: alpine:latest
  before_script:
    - apk add --no-cache curl
    - curl -L https://github.com/sigstore/cosign/releases/latest/download/cosign-linux-amd64 -o /usr/local/bin/cosign
    - chmod +x /usr/local/bin/cosign
  script:
    - echo "🖋️ Signing the image with Cosign (Validation Step)..."
    - cosign version
    - echo "Signing stage ready for production keys."
  only:
    - main

# 5. Herstart Servers
deploy-to-cloud:
  stage: deploy
  image: google/cloud-sdk:alpine
  before_script:
    - echo "$GCP_SERVICE_KEY" > gcp-key.json
    - gcloud auth activate-service-account --key-file=gcp-key.json
    - export GCP_PROJECT_ID=$(echo $GCP_PROJECT_ID | tr -d '\r\n')
    - gcloud config set project $GCP_PROJECT_ID
  script:
    - echo "🔄 Rolling update van de Managed Instance Group..."
    - gcloud compute instance-groups managed rolling-action restart conversey-mig --zone=europe-west1-b --project=$GCP_PROJECT_ID --quiet
  only:
    - main